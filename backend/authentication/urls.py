from django.urls import path
# from . import views

urlpatterns = [
    # We'll add these when we create the views
    # path('register/', views.RegisterView.as_view(), name='register'),
    # path('profile/', views.UserProfileView.as_view(), name='profile'),
]
